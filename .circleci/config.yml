version: 2.1

################# ORB #################
orbs:
  # See the orb documentation here: https://circleci.com/developer/orbs/orb/circleci/python
  python: circleci/python@1.4.0

################# WORKFLOWS #################
workflows:
  build_and_test:
    # For more details on extending your workflow, see the configuration docs: https://circleci.com/docs/2.0/configuration-reference/#workflows 
    jobs:
      - build-and-code-check
      - tests-no-kafka:
          requires:
            - build-and-code-check
      - tests:
          requires:
            - build-and-code-check
      - check-repo:
          requires:
            - build-and-code-check
            - tests-no-kafka
            - tests

################# JOBS #################
jobs:
  # Build the repo and check the code using pyflakes 
  build-and-code-check:
    docker:
      - image: cimg/python:3.7
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: pip
      - run:
          name: pip install
          command: cd ~/project; pip install .[all]; cd -
      - run:
          name: code check
          command: cd ~/project; python -m pyflakes .; cd -
  # Run unittests that don't interact with the kafka cluster at all in parallel
  tests-no-kafka:
    docker:
      - image: cimg/python:3.7
        environment:
	  PARALLEL_TEST_CLASSES: >-
	    test.unittests.testconsolescripts
	    test.unittests.testconfigfileparser
	    test.unittests.testargumentparsing
	    test.unittests.testcreatingkafkaproducersandconsumers
	    test.unittests.testserialization
    parallelism: 3
    steps:
      - run:
          name: run non-Kafka tests
          command: |
	    TESTCLASSES=$($PARALLEL_TEST_CLASSES | circleci tests split)
	    python -m unittest $TESTCLASSES
  # Run unittests that DO interact with the kafka cluster sequentially
  tests:
    docker:
      - image: cimg/python:3.7
    steps:
      - run:
          name: test datafilechunk
          command: python -m unittest project.test.testdatafilechunk
      - run:
          name: test datafile
          command: python -m unittest project.test.testdatafile
  # Make sure that the repository is clean after all tests have been run
  check-repo:
    docker:
      - image: cimg/python:3.7
    steps:
      - run:
          name: check repo
          command: cd ~/project; sh test/test_repo_status.sh
